1. babel 默认不会转 Object.assign
	 导致旧版浏览器（ Object 中无 assign 方法）中使用 Object.assign({}, {}) 时会报错

	 # npm install --save babel-polyfill

	 # import 'babel-polyfill';

2. # chmod +x the_file_name -> 追加权限

3. android 有个兼容 https 注入 cordova 的问题: 
	 在 crosswalk 以及 system webivew , applocal:// 无法注入
	 需要加多个语句  <script src="https://xxx/cordova.min.js"></script>  (xxx/cordova.min.js为本地空的 js 文件路径)

